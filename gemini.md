# Project Notes

- **Docker Builds**: Docker cannot be used to build images on this machine (command not found or no daemon).
- **Deployment**: Images must be built and pushed from another computer where Docker is available using the `./deploy.sh` script.
