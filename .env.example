# Mode of operation: MOCK (local simulation) or REAL (GKE Agent Sandbox)
MODE=REAL

# Name of the GKE Gateway resource for Production mode
GATEWAY_NAME=external-http-gateway

# Name of the GKE cluster
CLUSTER_NAME=dity-test-cluster

# Google Cloud Region
REGION=us-west1

# Google Cloud Project ID (Required for REAL mode)
PROJECT_NAME=gke-ai-eco-dev

# GEMINI API KEY (Required for Gemini API mode)
GEMINI_API_KEY=YOUR_API_KEY

# Set to TRUE to use Vertex AI instead of Gemini API
GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Name of the Cloud Storage bucket for storing Pod Snapshots (Optional, defaults to PROJECT_ID-sandbox-snapshots)
# SNAPSHOT_BUCKET_NAME=YOUR_SNAPSHOT_BUCKET_NAME